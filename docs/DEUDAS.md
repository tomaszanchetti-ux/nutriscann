# Registro de deudas — NutriScann

Lo que sabemos que falta, decidido conscientemente y con dueño. Una deuda que no
está escrita no es una decisión: es un olvido.

Cada deuda se cierra editando su fila a ✅ con la fecha, nunca borrándola: el
registro también sirve para ver qué se destrabó y cuándo.

| id | estado | título | dueño |
|---|---|---|---|
| DT-1 | ✅ cerrada 31/08 (WS04) | Activar el plan Blaze del proyecto: **activado por Tomás** y verificado (`billingEnabled: true`, cuenta `0151E4-2765DB-FC9340`) | Tomás |
| DT-2 | ✅ cerrada 02/09 (WS08) | **API key en Secret Manager, cargada por Tomás y verificada**: la versión 2 es la buena (probada a ciegas contra la API real — HTTP 200; el valor jamás pasó por el orquestador) y la versión 1, equivocada, quedó deshabilitada (el destroy definitivo espera el `gcloud auth login` de Tomás). El runtime y el deploy tienen sus permisos mínimos sobre el secreto | Tomás + Claude |
| DT-3 | ✅ cerrada 02/09 (WS08) | **Storage inicializado en `europe-west1` y reglas desplegadas por el CI.** ⚠️ Lección: el "Comenzar" de la consola creó el bucket en US-WEST1 (el selector viene con EE.UU. por defecto); se detectó recién creado y vacío y se recreó vía API en la región de las Functions — cero datos perdidos | Tomás + Claude |
| DT-4 | ✅ cerrada 31/08 (WS04) | Presupuesto de GCP: **creado** — "NutriScann — presupuesto mensual", €10/mes, alertas al 50 % y 90 %, acotado al proyecto | Tomás + Claude |
| DT-5 | ✅ cerrada 01/09 (WS07, card 3.4) | **Íconos de la PWA completos, sin resto**: el donut de macros entre las cuatro esquinas del scanner (cita el lenguaje visual existente, tokens del design system, el morado fuera porque a 16 px se empasta). SVG fuente + maskable + 7 PNG (`rsvg-convert`, cero dependencias npm; `hasAlpha: no` verificado — iOS exige el apple-touch sin alfa). `manifest.webmanifest` reescrito (standalone, colores de tema, 5 íconos, los `src` verificados en disco) y `index.html` con los links + metas de iOS | Claude |
| DT-6 | 🟡 diferida → **v2** | Recalibrar la regla de azúcares (totales vs libres) y el sodio con datos reales de uso; resolver en la UI el caso "el tag acusa y el texto absuelve". *Reencuadrada 31/08: la v1 no muestra recomendaciones (decisión Tomás), así que esta calibración pertenece al esquema de recomendación de la v2* | Claude (v2) |
| DT-7 | ✅ cerrada 30/08 (WS03) | Desambiguar los pares FNDDS/SR de nombre casi igual: **ejecutada** — censo de 137 pares (`kb/selection/dt7.pairs.json`), 8 fusiones, 27 renames bajo "el nombre nunca miente", con candado de regresión. Lo que quedó ambiguo pasó a DT-8 | Tomás + Claude |
| DT-8 | ✅ cerrada 31/08 (WS04) | **7 pares resueltos por decisión de Tomás** (registro completo en `dt7.pairs.json`): los 2 parmesanos se conservan (diferencia real de sodio; de paso se corrigió la porción errónea de 100 g de fdc-171247 → 5 g) · puré de papa fusionado al casero · pepinillos sin cambio (asunto del matcher, Fase 2) · mantequilla NFS conservada bajo la política DT-13 · frijoles y croquetas duplicados idénticos fusionados con herencia de vocabulario | Tomás + Claude |
| DT-9 | 🟡 diferida | Censo de duplicados **intra-fuente**: eran 36 grupos con `per_100g` idéntico dentro del mismo dataset; **la DT-8 ya fusionó 2 de ellos** (frijoles, croquetas) — quedan ~34. La DT-7 solo cubrió los cruzados | Claude (WS05+) |
| DT-10 | 🟡 diferida | Rendimientos de cocción sin fuente suficiente: curado de **lomo** (el par medido es de jamón, 0,784), verdura asada A LA BRASA (5 pares en 2 familias que se contradicen; **la card 6.4b sí midió la COCCIÓN de la cebolla —`cocido_cebolla` 0,850, n=2— y con eso destrabó los calçots por la vía del horno, pero la brasa sigue sin factor**) y `hervido` (publicado 1,113 con dispersión 0,71–1,47; ninguna receta lo usa — la que lo necesite declara el suyo). Cada uno desbloquea fichas concretas | Claude |
| DT-11 | 🟡 diferida | **Pulpa de açaí congelada** y **lomo embuchado**: sin derivación defendible (derivar la pulpa del polvo sería elegir la dilución que dé el número esperado). Se resuelven con la cola de curación y uso real | Claude (runtime) |
| DT-12 | 🟡 diferida | El CI corre los candados en frío pero **no** el circuito del emulador del seed (necesita el emulador de Firestore + Java en el runner) | Claude |
| DT-13 | ✅ cerrada 31/08 (WS04) | **Política de genéricos decidida por Tomás y ejecutada**: los nombres quedan matcheables; toda ficha genérica (marcadores NFS / "NS as to" de USDA) lleva `generic: true` (339 fichas — el matcher de la Fase 2 les baja la confianza) y las de sodio ≥ 400 mg/100 g un caveat generado por regla declarativa con su valor real (101 fichas). Regla en `kb/curation/genericos.dt13.json`, candado bidireccional en el build. El censo real resultó mayor que el enunciado original (101 caveats, no 4 casos) | Tomás + Claude |
| DT-14 | 🟡 diferida | Los **9 gemelos de confianza 0,5** (milanesa, cocido madrileño, callos, patatas bravas…) son candidatos naturales a ficha por **receta compuesta** (mecanismo 1.7): pasar de aproximación con reserva a receta real | Claude (WS04+) |
| DT-15 | ✅ cerrada 31/08 (WS04, card 2.1) | El matcher resuelve las colisiones declaradas, verificado adversarialmente: familia "Pastel" (especificidad por tokens), cruce "Catsup" (índices EN/ES separados, el mejor candidato gana y el inglés solo desempata), pepinillos (guarda con excepción "dulces"), "chorizo"/"Bife de chorizo" (guarda de runtime + barrido de 1.022 fichas con determinante: 0 desvíos). Lo que queda de recall difuso (plurales, tokens desordenados) vive en **DT-17** | Claude |
| DT-16 | 🟡 diferida | Alias "Callos" duplicado en `fdc-2706162` (aparece a confianza 1,0 y 0,5 en la misma ficha). Preexistente en `main` desde la Fase 1, detectado por el Q/A de la WS04 | Claude (WS05+) |
| DT-17 | 🟢 núcleo ejecutado 01/09 (WS05, card 2.6) | **El recall EN/ES se ejecutó**: visión bilingüe (`food_es` en el schema), variantes de índice para los marcadores USDA, cobertura por núcleo (el piso 0,30 no se movió), plurales plegados y la regla crudo/cocido preguntada a los datos (entre cruda y cocida gana la cocida solo si la cruda tiene más calorías). Sobre los 17 términos grabados: 5 → 14 matches, cero fichas mal elegidas; barrido de 90 términos: +24 matches, 0 perdidos. Lo que queda vive en DT-26 (curación) y en el texto original: recall y robustez del matching difuso (hallazgos menores del Q/A de la card 2.1, todos con falla cerrada — van a curación, no inventan): **plurales/singulares no matchean** ("pepinillo", "chorizos" → sin match; el hueco de recall más caro en español) · tokens desordenados pueden confundir ("carne pastel" → Tarta a confianza 0,3) · sin techo de gramos (1e12 g se reporta sin reserva — poner un límite de cordura en el endpoint o el engine) · `interpretarVision` (2.2) colapsa "no estimó" y "gramos imposibles" en 0 antes del motor, así que la distinción fina de F5 del engine no puede dispararse desde el pipeline real (las dos salidas son honestas; es granularidad perdida, no un error) · **un término multi-palabra que CONTIENE un alias exacto no dispara la vía del alias** (medido en el primer E2E real, 01/09: "spaghetti carbonara" no encontró el alias "Carbonara" a 0,5 de la ficha correcta; el difuso llegó a la MISMA ficha pero mostró 12 % en vez de ~42 % — el resultado es correcto, la confianza que ve el usuario está injustamente baja) | Claude (v1.1, con datos de la cola de curación) |
| DT-18 | ✅ cerrada 01/09 (WS05, card 2.5) | **`copy` gobernado por el seeder**: `config/copy.json` nace con las 18 claves (5 de la Fase 0 + 13 nuevas), byte a byte con el arranque en frío del front; máscara de 4 → 5 campos, el campo se publica ENTERO (una clave agregada a mano en la consola desaparece en la corrida siguiente — regla 3). Verificado E2E: el pie pasó de "arranque en frío" a "Firestore". ⚠️ Al primer seed real, `scanning_steps` cambia 2 de 3 pasos respecto de lo publicado a mano en la Fase 0 (gana el texto del repo) | Claude |
| DT-19 | 🟡 diferida | Los platos **compuestos** salen con `name_es: null` — el reporte muestra el término de la visión en inglés ("Chicken and pepper skewer"). Decidir el mecanismo del nombre en español (¿la visión lo devuelve bilingüe? ¿se compone de los nombres ES de los ingredientes?) | Claude (Fase 3) |
| DT-20 | ✅ cerrada 01/09 (WS07, card 3.1) | **Los tipos del front alineados al payload real, CON candado**: `lib/types.ts` suma `termino_es`, `identidad_respaldada`, `grams_no_estimados` y los 8 nutrientes nullables + `total_no_publicable`. El candado vive en `kb/seed/src/contrato-front.test.ts` (apps/web no tiene runner y sumarle uno pedía dependencias — mismo motivo que `textos.test.ts`): lista de campos + texto de cada tipo normalizado sin comentarios, campo por campo contra `functions/src/engine/types.ts` y `kb/src/types.ts`. Probado que muerde (volver `kcal` a `number` lo hace fallar) | Claude |
| DT-21 | ✅ cerrada 01/09 (WS07, card 3.4) | **Pasada de España ejecutada como corrección, no como estilo** (cada cambio con antes/después en el reporte de la card): 7 de las 18 claves originales voseaban (sacá/probá/revisá → haz/prueba/revisa; acá → aquí), los 6 tooltips de match pierden la jerga interna ('ficha'/'catálogo' → 'la base nutricional', el nombre que los scanning_steps ya usaban), la vitrina premium ya estaba bien y no se tocó. Los restos estructurales que la pasada descubrió → **DT-40** | Claude |
| DT-22 | ✅ cerrada 01/09 (WS07, card 3.1) | **29 claves nuevas a `config/copy.json` (18 → 47)**: nutrientes, leyendas del donut, los 5 tipos de match con sus ayudas, estados sin total. Espejo byte a byte con `lib/config.ts` verificado programáticamente. Línea declarada: viaja lo que es frase fija; queda en código lo que se arma con datos (copy no tiene convención de placeholders). `copy.premium.ts` (card 3.3) y el 4.º paso del escaneo quedan para la 3.5, que además debe re-sembrar: hasta ese seed las 29 claves salen del arranque en frío, idéntico byte a byte | Claude |
| DT-23 | ✅ cerrada 01/09 (WS07, card 3.1) | El título de macros huérfano (`report_macros_title` solo se veía cuando NO había macros) encabeza ahora el primer recuadro del reporte: donut + macros con gramos. El segundo recuadro agrupa los menores (fibra, saturadas, azúcares, sodio) + peso identificado — el criterio unificado que pidió Tomás | Claude |
| DT-24 | 🟡 diferida | **La cola de curación no distingue "no lo tengo" de "no lo supe encontrar"**: en el test de 10 platos, 22 de los 30 términos encolados tenían ficha directa en el catálogo. Con el recall de la card 2.6 el ruido debería bajar; medir de nuevo y, si persiste, anotar en el candidato si el catálogo tenía algo parecido | Claude (v1.1) |
| DT-25 | ✅ cerrada 01/09 (WS07) | **`termino_es` viaja al expediente, SIEMPRE presente** (`""` = la visión no lo dijo; clave ausente = scan anterior a esta deuda — la ambigüedad que había que evitar). `persistencia.ts` no necesitó cambios (guarda `resultado.items` entero); candado de forma de `EngineItem` actualizado. **El replay bifurca sobre la presencia de la clave**: con ella re-juega `buscarConDosNombres` (la función que decidió el match aquel día) y no queda ni un `no_comparable_es`; las corridas v1–v3 siguen saliendo `no_comparable_es` con test explícito que lo exige — reconstruir el término sería inventar la entrada. La primera corrida que aprovecha el campo es la v4, que por fin podrá medir el duelo de idiomas de DT-37 | Claude |
| DT-28 | 🟢 núcleo ejecutado 01/09 (WS06, card 6.1) — falta la re-corrida | **Los 5 mínimos ejecutados y medidos**: (1) cola descriptiva EN **y ES** con variantes solo aditivas (candado explícito: `sin grasa`/`without salt` NO son cola — la primera versión convertía mayonesa 680 en light 64 y se cazó antes de entregar; barrido de 24.677 consultas: 0 perdidos) + desempate "término escrito le gana a variante deducida" · (2) la barra `/` se lee como O (jamón `serrano/ibérico`: 0,297 difuso → alias 0,8; ⚠️ evidencia de test, no de replay — el expediente no guarda `food_es`, ver DT-25) · (3) compuerta cerrada → los 8 nutrientes en `null` + `total_no_publicable: true` (tipos públicos separados de la suma interna; el front no rompe en runtime pero DT-20 quedó urgente) · (4) criterio 2 recalibrado a regla de tres sobre gramos reportados: v2 pasa de 76,9 % ❌ a **88,5 % ✅** (no el 96,2 % estimado a mano: ajustar el rango también SACA al plato 16, que pasaba por dos errores compensados) · (5) estabilidad de la visión medida v1→v2: **17 de 30 platos se mueven**, salto absoluto promedio del total 8,5 %, peores −46 %/−32 %/−25 % — la visión decide más juicios que el motor. **El golden set vive en `golden/` del repo** (7 MB, fotos ya a 1.024px/JPEG80, bodies gitignorados regenerables, replay offline con detección de idioma del motivo — "perdidos 12-15" era artefacto de medición). Solo 2 de los 5 silencios eran de cola (medido): `sweet corn`/`cabbage` son curación, `grilled potato slice` calla correctamente. **Falta: la re-corrida real buscando 5/5 (card 6.5)** | Claude (WS06) |
| DT-27 | ✅ cerrada 01/09 (WS06, card 6.2) | **Lote de fichas nuevas ejecutado: 14 fichas, catálogo 3.2.0+e19bb31f (1.036 alimentos), aditividad verificada por diff (0 retiradas, 0 preexistentes cambiadas)**: cerveza ×3 (regular 43 · light 29 · alta graduación 58, con el vocabulario de barra completo: corto/caña/tercio/doble/jarra/litrona) · vino tinto/blanco (ganó USDA: copa 150 ml = 127/122 kcal, con densidad real 0,994 — la conversión "104 kcal/45 ml" del insumo trataba ml como g) · destilado 80-proof (copa 45 ml = 99 kcal, densidad 0,947) · limón (29) · **arepa fdc-168070 "Restaurant, Latino, arepa" (219): la premisa "sin ficha USDA posible" era FALSA** — lo verificado era que el motor no la encontraba; la receta compuesta se descartó por 3 grados de libertad movibles (el defecto del açaí) · tortilla de maíz (218, cierra la regresión de DT-26) · pechuga de pollo ×3 (genérica/horno/plancha) · pita · alubias en salsa de tomate. **NO fichadas con motivo medido**: cerveza 0,0 (USDA solo tiene "Malt beverage", 4-7× off) y cerveza negra (no existe en los datasets; el rango del insumo no describe a la negra real). Alias "Vino" a secas no emitido (desempate arbitrario → cola de curación). Caveat de alcohol → DT-31. Guardas 5→7, dorados 6→9, candado nuevo "catálogo commiteado = salida del pipeline" | Tomás (producto) + Claude |
| DT-29 | 🟡 diferida → **v2** | **Hierbas y especias fuera de la v1** (decisión Tomás 01/09, WS06): no son guarniciones — son aditivos/complementos de los platos — pero sin impacto calórico relevante no ameritan ficha en v1 (albahaca, azafrán, laurel, orégano, comino… no existen en el catálogo y caen a `no_catalogado`). En v2 considerar su incorporación pensando en dietas específicas (ej. pimienta + cúrcuma = efecto antiinflamatorio) — conecta con el esquema de recomendación v2 | Tomás (producto, v2) |
| DT-30 | ✅ cerrada 01/09 (WS07) | **El generador ya no puede resucitar un alias retirado**: `$extra` limpiado (`Tira de asado` fuera de fdc-169510) y candado declarativo nuevo `$retirados` en `variants.es.json` (por `fdc_id`, con `retirado_por` y motivo; hoy `Filete` fdc-2705824 y `Tira de asado` fdc-169510), que `build_aliases.py` aplica en las dos direcciones: no lo emite y lo SACA de `names.es.json` si alguien lo escribió a mano, avisando por stderr. El generador honra además `guardas.vocabulario.json` (hoy no-op, mañana candado). `--check` en VERDE — fallaba por 6 entradas, no 2: las otras cuatro eran alias legítimos que ninguna corrida había escrito (choclo/chauchas, que de paso corrigieron su ficha "sin grasa": ahora resuelven a 1,0 a la ficha correcta — un pedazo del hallazgo español de DT-39) más un falso positivo `Nata de berenjena` que salió por `$skip`. Test de mutación verificado: inyectar los dos alias a mano hace fallar `--check` y la corrida normal los borra | Claude |
| DT-31 | 🟡 diferida | **Caveat "calorías del alcohol" para las 6 fichas de bebidas**: el candado 1 prohíbe caveats manuales en fichas USDA salvo los generados por regla declarativa (patrón DT-13). Ponerlo exige una regla nueva en `locks.ts`+`canonical.ts`+`curation.ts` con re-derivación bidireccional — cambio de diseño, no una ficha. Mitigado: el candado de Atwater ya suma 7 kcal/g de alcohol y tiene test de mordida | Claude (v1.1) |
| DT-32 | ✅ cerrada 01/09 (WS07) | **Las guardas de vocabulario ya MUERDEN en el matcher, patrón regla 1**: el build emite `guardas` como cuarta clave de `foods.canonical.json` (entran al hash — catálogo **3.8.0**, diff de fichas VACÍO), `construirIndice` las lee de ahí y la constante del motor queda como arranque en frío declarado; el seed las publica en `config/kb_meta` y `cargarIndice` las lee en producción (sin la clave: arranque en frío + `logger.warn` con el remedio). `pepinillos` se mudó a la curación con su `salvo_si_contiene` (era la única guarda solo-motor). Los tres casos medidos cerrados: `pasta de tomate`, `pasta filo` y `huevas de salmón` → SILENCIO (el destino correcto). **El barrido de 28.116 consultas cazó y arregló daño colateral real**: `ajo porro` (puerro colombiano) caía en Ajo crudo por la asimetría build (igualdad) / matcher (prefijo) — excepción `porro` medida, barrido final 0 diferencias. **Método obligatorio escrito en el JSON: toda guarda nueva cuyo término sea prefijo de un nombre legítimo necesita su excepción, y eso solo lo descubre el barrido.** ⚠️ Hasta el próximo `kb:seed`, producción corre con el arranque en frío (2 guardas) — re-sembrar en la card 3.5 | Claude |
| DT-33 | ✅ cerrada 01/09 (WS06, card 6.4) | **Ejecutada: 76 fichas nuevas, catálogo 3.4.0 (1.112 alimentos), aditividad verificada por diff (0 retiradas, 0 preexistentes cambiadas byte a byte)**. Los SIETE huecos centrales están tapados y seis de ellos resultaron estar medidos por USDA desde siempre —lo que faltaba era la promoción, no el dato—: **salmón ×4** (fdc-2706285 `Salmón` 274 · a la plancha 259 · ahumado 117 · crudo 188, este último caso dorado nuevo) · **mejillones** (109) · **pez espada** (172, de SR: el de FNDDS lleva 14,9 % de rebozado) · **pasta cocida** (157, caso dorado nuevo, destraba `spaghetti, cooked` por el lado español) · **harina de trigo** (364) · **maíz dulce cocido sin grasa** (86) · **repollo verde cocido sin grasa** (32). Más 26 fichas de despensa y casquería mediterránea (alcachofa, alcaparras, habas, cardo, calabacín, judías verdes sin grasa, conejo, riñones, mollejas, hígado, codorniz, faisán, paletilla de cordero, panceta, anguila, calamar, caracol, cangrejo, bogavante, langosta, tahini, ensalada griega, crema de berenjena…). Y **43 recetas compuestas** que cierran 43 de los 48 platos `ausente_ficha`. **El censo pasó de 38 a 85 platos `ok`, de 48 a 5 `ausente_ficha` y de 5 a 0 `ficha_equivocada`**; los ingredientes, de 37 a 45 `ok` y de 59 a 44 `ausente_ficha`. Regresión: barrido de 3.222 términos (todo el vocabulario del catálogo viejo + los 141 platos + los 196 ingredientes + los 93 términos del golden set), **0 perdidos, 0 confianzas bajadas, 62 matches nuevos y 7 cambios de ficha, los 7 correcciones**. Los 5 bloqueos que quedan y las deudas nuevas están en **DT-35** | Claude |
| ~~DT-33~~ | *(texto original, conservado)* | **Huecos de ficha que destapó el censo de cobertura mediterránea** (`kb/cobertura/censo.json`, card 6.3) — es el insumo de la card 6.4, y estos son los que NO son de curación sino de catálogo: **no hay NINGUNA ficha de salmón** en las 1.036 (medido: cero coincidencias con `salmon`/`salmón`; el único acierto es `Lomi salmon`, que es un plato hawaiano), **ni de mejillón, ni de pez espada, ni de pasta cocida simple** (`spaghetti, cooked` ya daba silencio en el golden set; lo único que hay es `Pasta seca enriquecida`, cruda, y `Pasta con salsa`), **ni de harina de trigo** (hay de papa, arroz, soja, garbanzo, mijo, arrurruz, malta y trigo sarraceno). Se le suman los dos pendientes que la card 6.1 había mandado a curación y resultaron ser de ficha (ver DT-26): **maíz dulce** (ninguna ficha en el catálogo) y **repollo cocido sin grasa añadida** (solo está el de grasa añadida, 55 kcal, contra las ~23 del hervido). 48 de los 141 platos quedaron `ausente_ficha`, con los cinco `ficha_equivocada` que necesitan ficha propia además de su guarda ya puesta | Claude (card 6.4) |
| DT-34 | 🟡 diferida | **Huecos de vocabulario que el censo midió y la card 6.3 decidió NO curar**, con el motivo escrito en `censar.js`: `huevos`/`patatas`/`espaguetis`/`harina` a secas caen mal porque no dicen la PREPARACIÓN, y elegirla por decreto sería inventar (en el pipeline real la visión siempre la escribe — es el hueco de plurales de la DT-26) · `pimentón` cae en `Pimiento rojo crudo` por el alias americano `Pimentón rojo crudo`, y desambiguar a favor de España rompería el vocabulario de América (además las especias están diferidas a v2 por DT-29) · `puré instantáneo` da silencio teniendo `Puré de papa instantáneo` en el catálogo · `filete` volvió a encontrar carne vacuna por otra puerta (`Filete de ojo`, fdc-2705828) después de que la card 2.7 lo extirpara de fdc-2705824 · `Buñuelos de viento` y `Soldaditos de Pavía` son candidatos limpios a la regla de herencia de nombre largo que estrenó la 6.3 | Claude (curación) |
| DT-35 | 🔴 abierta | **Lo que la card 6.4 dejó fuera, con el motivo por caso.** (a) **CINCO platos bloqueados de los 141** — ⚠️ **AHORA TRES: la card 6.4b (WS06) destrabó los `Calçots` y la card 6.4c destrabó el `Torrezno de Soria`.** Los `Calçots` tienen ficha (`receta-calcots`, 37,6 kcal/100 g) por el transform nuevo `cocido_cebolla` (0,850), que es el rendimiento que la propia FNDDS le asigna a la cebolla, recuperado de su `input_food` con n=2 pares concordantes; la vía declarada es el HORNO a 200 °C y la ficha se publica como SUELO, porque la cocción genérica de FNDDS pierde menos agua que un asado. El **`Torrezno de Soria` YA NO ESTÁ BLOQUEADO: la card 6.4c lo destrabó por OTRA PUERTA, la ETIQUETA** — `manual-torrezno-de-soria` sale de una etiqueta comercial verificada campo a campo, la misma vía de la salsa de calçots; **desde la WS07 la etiqueta es la Carrefour 8431876311617 (PANCETA, 627 kcal/100 g — la mediana del mercado en cuatro campos, Atwater al 0,16 %), por decisión de producto de Tomás: la Hacendado 8480000334169 (careta, 580) era el extremo magro y quedó nombrada en los caveats para poder revertir**. **La DT-36 NO se cierra con eso**: el modelo sigue sin saber restar la grasa que sale de la pieza y el rendimiento 0,403 sigue fuera de la tabla de transformaciones. **Los TRES que quedan son ahora el mismo caso**, que es lo que cambia el mapa: los tres están bloqueados por ESPECIE o INGREDIENTE y los tres esperan la misma llave, la pasada de BEDCA del punto (g). Siguen bloqueados `Perdices estofadas`, `Besugo a la espalda` y `Ensalada Halloumi` por ESPECIE/INGREDIENTE — `partridge`, `porgy`/`sea bream`/`bream` y `halloumi` dan CERO coincidencias en los tres datasets. (b) **`spaghetti, cooked`, `sweet corn, cooked` y `cabbage, cooked` SIGUEN dando silencio aunque la ficha ya exista**, y ya no es un hueco de catálogo sino el límite de la DT-26: el `names.en` de una ficha de USDA lo escribe el CSV (`Pasta, cooked`, `Corn, fresh, cooked, no added fat`, `Cabbage, green, cooked, no added fat`) y la curación no lo puede tocar — por el lado español los tres resuelven a 1,0 (`espaguetis cocidos`, `maíz dulce cocido`, `repollo cocido sin grasa`). Es del matcher o de una extensión del contrato de curación al inglés. (c) **Dos falsos positivos NUEVOS que trajo `Pasta cocida`**: `pasta de tomate` (0,25) y `pasta filo` (0,30) antes daban silencio y ahora caen en la pasta por la palabra compartida; sus guardas están escritas y **desde la WS07 MUERDEN (DT-32 cerrada): los dos vuelven al silencio, que es el destino correcto**. (d) **`gallina`** cae en el PLATO `Gallina en pepitoria` y USDA no mide la gallina como especie. (e) **`huevas de salmón`** cae en `Salmón`: USDA sí las mide (`Fish, roe, mixed species`) y se decidió no promoverlas — un solo uso en las dos fuentes. (f) Vocabulario listo para ejecutar que esta card NO tocó por su regla de aditividad estricta (habría cambiado fichas preexistentes): alias `Besugo` sobre `Lubina`, `Chopitos` sobre `Calamares rebozados`, y la herencia de nombre largo para `Paletilla de cordero lechal al horno o lechazo`. (g) **BEDCA quedó SIN abrir, y con el motivo medido**: la card 6.4 tenía autorización para usar la Base Española de Datos de Composición de Alimentos (bedca.net, la que el `docs/PLAN.md` §1 D5 ya nombraba como complemento) y no pudo: su consulta pública es una aplicación JavaScript sin HTML consultable —todos sus enlaces son `javascript:void(0)`— y su webservice espera un POST con cuerpo XML, que las herramientas de la sesión no pueden emitir. Tres de los cinco bloqueos (**besugo**, **perdiz** y **halloumi**) son alimentos que BEDCA sí publica y que USDA no mide: son el candidato natural de la próxima pasada, con provenance `bedca` declarado campo a campo y sin mezclarse con USDA dentro del mismo campo, igual que hace `manual.foods.json` con el salmorejo. Los otros dos (calçots, torrezno) NO se destrababan con una fuente nueva: les faltaba un rendimiento de cocción, no un número — **y los dos ya se destrabaron, cada uno por su vía: el calçot con un rendimiento medido (6.4b) y el torrezno con una etiqueta comercial (6.4c), sin que ninguno cerrara la DT-36**. Con eso, los tres pendientes de esta deuda son los tres de BEDCA y ninguno más | Claude (WS07) |
| DT-36 | 🔴 abierta | **El modelo de recetas no sabe restar la grasa que SALE de la pieza, y eso es lo único que bloquea al `Torrezno de Soria`** (medido en la card 6.4b, WS06). `kb/src/transforms.ts` deriva con `nutriente = Σ nutriente_ingrediente / peso_final`: supone que lo ÚNICO que se va al cocinar es AGUA, y por eso un `factor_peso` concentra TODOS los nutrientes por igual. Es cierto para un horneado de carne y es FALSO para una fritura de panceta: con el par medido de USDA (fdc-168277 → fdc-168322, rendimiento 0,403 con la proteína como trazador) la grasa retenida es apenas el 38 % —casi dos tercios se van al fondo de la sartén— mientras que el sodio y la ceniza retienen el 90 % y el 88 %, que es lo que confirma que el rendimiento está bien medido. Aplicar 0,403 a `Panceta cruda` (fdc-167812) daría **131,5 g de grasa y 1.285 kcal por 100 g**: más de 100 g de materia dentro de 100 g de alimento. Los dos candados lo frenan (rendimiento 0,403 < `RECIPE_YIELD_MIN` 0,50 · densidad 1.285 > `RECIPE_KCAL_MAX` 900) y hacen bien. **Se probó también la salida barata y se descartó por circular**: extender el modelo con la retención de grasa del propio bacon (38 %) da 542 kcal/100 g y 26 % de agua, que es la composición del bacon crocante con nombre español — y queda POR ENCIMA del bacon frito de USDA (468 kcal) cuando el torrezno, que conserva la carne jugosa, tiene que quedar por debajo. **Qué hace falta**: una transformación con retención POR NUTRIENTE (USDA publica `retention_factor.csv` en SR Legacy y FNDDS referencia códigos de retención en su `input_food`) más un dato de retención de grasa propio del CORTE GRUESO, que no está en los tres datasets. Mientras tanto el rendimiento medido queda publicado en `kb/curation/cooking.transforms.json` → `$transformaciones_que_NO_estan_y_por_que.fritura_de_panceta`, y el candado `card64b.test.ts` verifica que NO esté en la tabla de transformaciones usables. **⚠️ La card 6.4c (WS06) publicó la ficha del torrezno POR ETIQUETA (`manual-torrezno-de-soria`, 580 kcal/100 g, Hacendado 8480000334169) y ESTA DEUDA SIGUE ABIERTA TAL CUAL: el plato dejó de ser un hueco del censo, el MODELO no cambió ni una línea.** Dos matices que la 6.4c midió y que esta deuda hereda: (1) el descarte del extremo seco (542 kcal) se argumentó con «queda por encima del bacon frito de USDA (468) y el torrezno tiene que quedar por debajo», y las 26 etiquetas de mercado contrastadas dicen que el torrezno frito real va de 552 a 665 kcal (mediana 627) — **542 era un PISO y no un techo, así que el modelo extendido con la retención del bacon no se pasaba de alto sino que se quedaba corto, y el criterio de aceptación de la retención por nutriente hay que reescribirlo con esa banda**; (2) el día que el modelo sepa derivarlo, la ficha derivada y la de etiqueta van a convivir y hay que decidir cuál gana — hoy la manual tiene la precedencia más alta del pipeline y la pisaría | Claude (WS07) |
| DT-37 | ✅ cerrada 01/09 (WS07) | **Los dos defectos arreglados, con tests que reproducen los casos medidos** (18 nuevos; suite 337/338, 1 skip): (1) **la compuerta tiene una SEGUNDA puerta** — `respaldoDeIdentidad` mide si el vocabulario ENTERO de la ficha (en+es+alias) nombra lo que la visión describió, umbral 0,6 elegido sobre el histograma de las 3 corridas (hueco entre 0,50 y 0,75; ante la duda el piso se sube), y exige además `confidence_vision ≥ 0,12` — la lasaña del 05 vuelve a publicar su total (724,5 kcal, en rango 450–850) y el plástico del 28 sigue mudo (los `Miel` no tienen respaldo); (2) **`buscarConDosNombres` detecta la CONTRADICCIÓN**, no el score: si la ficha del español no explica NI UNA palabra del término inglés, gana el inglés solo cuando su ficha lo explica entero (`lime`→Lima) o cuando los dos son difusos (`gravy`→Salsa de carne); el español CURADO (exacto/alias) sigue ganando aunque no comparta palabras. Blast radius medido: 0 cambios en 1.115 pares de nombres propios, 1 cambio en los 176 pares grabados (lime), 0 cambios en 90 corridas-plato solo-inglés, los 36 ítems españoles intactos. **Restos declarados**: la confianza del acierto BAJA (gravy 0,09 · lime 0,255 — es el número honesto, pero UX a mirar) · el respaldo mide palabras, no significados (`Cóctel` contaría para `cocktail sausages`) · la mitad de visión de la compuerta (0,12) no la ejerce ninguna corrida · el duelo de idiomas solo se verifica con términos RECONSTRUIDOS hasta que DT-25 guarde `food_es` — el mejor argumento para cerrarla antes de la corrida v4 | Claude |
| DT-38 | ✅ cerrada 01/09 (WS07) | **Los dos parches aplicados a `criterios.json`**: la arepa (09) espera su ACIERTO (150 g previstos, rango 250–450 desde su ficha fdc-168070) y el bocadillo (27) mete las dos cañas en las DOS puntas de la cuenta (720 g previstos, rango 450–770 — los números de `predicciones.md` §27 que nadie había transcripto; excluir la cerveza ítem por ítem pediría tocar `golden.ts`, límite anotado). **Criterio 2 de la v3: 25/26 = 96,2 %** (solo el 16-jamón fuera, hasta la corrida v4). Efecto declarado en el `$doc` y en el candado `golden.test.ts` (actualizado): la vara re-juzga las corridas viejas y la v2 baja a 21/26 = 80,8 % sin que nada del producto empeore — sigue pasando el umbral. **La cláusula del 70 % SE QUEDA COMO ESTÁ — decisión de Tomás (01/09, tarde)** | Tomás |
| DT-39 | 🟢 núcleo ejecutado 01/09 (WS07) | **Ejecutado**: alias **`Jamón ibérico`** → Jamón crudo (fdc-2705879) a confianza **0,6** vía `aliases.regional.json` — no 0,8 como el serrano: el ibérico es otra raza y otro engorde y ningún dataset del repo lo mide (**0,6 CONFIRMADA por Tomás el 01/09**); medido: `jamón ibérico` pasa de la ficha EQUIVOCADA (el cocido, 93,6 kcal en 80 g) a 156,0 kcal, dentro del rango del plato 16 — que vuelve a ✅ recién en la corrida v4, porque el criterio lee los totales grabados · el maíz y las judías "sin grasa" del lado ESPAÑOL quedaron resueltos de rebote por el candado de DT-30. **Queda**: `corn, sweet, cooked` por el inglés (es DT-35 b mordiendo) · `beet, pickled` → remolacha cruda existiendo la de lata (+44 %) · `flatbread` sin ficha real (+89 %) · la manteca del 15 que la visión no ve y el motor publica `completo: true` (¿sospecha declarable?) · `receta-huevos-rotos` sin ganar en tres corridas (decisión de producto) · el alias `Queso manchego` sobre el parmesano sigue vivo · el salmón de la 6.4 sin foto que lo mida (candidata a foto nueva del set) | Tomás + Claude |
| DT-40 | 🟢 (a) cerrada 02/09 (WS09, card 4.5) · **(b) sigue abierta y ahora se sabe por qué** | **(a) CERRADA**: las 8 claves de error del backend entran a la lista cerrada de `config/copy.json` (64 → 106 con lo de DT-41) y el español queda corregido — incluido «pedido» → «petición», que en España no era voseo sino otra palabra. **El candado creció**: `textos.test.ts` compara contra la UNIÓN de los dos lectores (los campos del front más las `clave_copy` que declara `errores.ts`, leídas del archivo), así que un error nuevo cuyo texto no se siembre rompe el seed; probado rompiéndolo 5 veces. **(b) NO se hizo, con el motivo MEDIDO** (se aplicó de verdad y se revirtió; el catálogo está idéntico a HEAD): la frase «Ficha genérica:» tiene **TRES** puntas y no dos —el front, `kb/curation/genericos.dt13.json` y `functions/src/engine/analyze.ts:159`—, y cambiar la plantilla mueve la `kb_version` (3.8.0+843ecb80 → +ca09886b), que va estampada en cada documento: **el re-seed reescribiría los 1.115 documentos de producción por un cambio de palabras**, cerrando la v1. Receta completa para hacerlo de un solo movimiento: los tres textos + rebuild + re-seed + bump a 3.8.1 en `kb/src/canonical.ts` + aceptar los dos prefijos en el detector de `LetraChica` durante la ventana entre seed y despliegue | Claude, post-v1 |
| DT-41 | 🟢 cerrada 02/09 (WS09, card 4.5) salvo el resto de (b) | **(a) CERRADA**: el umbral de sodio sale de `ItemDelPlato.tsx` y se publica en `config/app.thresholds` (nace `config/thresholds.json`, máscara del seed 5 → 6 campos), con un candado nuevo que **compara el número publicado contra el `umbral_sodio_mg` de la curación**: separarlos rompe el test. **(b) PARCIAL**: 39 claves mudadas **byte a byte** (verificado contra el HEAD anterior: cero reescrituras encubiertas), entre ellas **la pantalla de planes entera** — precio, período, cupo, viñetas y sellos de los tres planes: *cambiar 12 € por 15 € ya no exige desplegar*. El resto (términos, pantalla de entrada, perfil, modales, navegación) encaja igual de bien pero necesita que **siete pantallas reciban `copy`**, y eso toca `App.tsx`, `api.ts` y `listaDeEspera.ts`: mudar la mitad habría dejado textos publicados que nadie lee. → **DT-46**. **(c) CERRADA**: 5 claves huérfanas y no 3 (`donut_unexplained` y `donut_rest` también lo estaban y nadie lo había notado). **(d)** ya salió con el despliegue de la WS08. **(e)** quedó en el Q/A visual de Tomás. **(f) CERRADA**: podada la maquinaria del anillo exterior (`DonutMacros.tsx` 456 → 271 líneas). Revivirlo sería trivial y los datos siguen viajando, pero es decisión de producto | Claude (cerrada) |
| DT-42 | 🔴 abierta (WS08) | **Los T&C viven en DOS lugares**: `apps/web/src/lib/copy.terminos.ts` (la app) y `apps/landing/terminos.html` (la landing, copiado palabra por palabra el 02/09). Si el texto legal cambia, hay que cambiarlo en las dos puntas o unificar la fuente — hoy no hay candado que avise la divergencia. Emparentada con la mudanza de DT-41 (b): si los T&C van a `config/`, la landing podría leerlos de ahí | Claude (Fase 4+) |
| DT-43 | 🟡 diferida (WS08) | **Cosecha menor de la marca CaliScan**: (a) la landing no tiene menú móvil — por debajo de 860 px los 4 tabs no se ven y se navega por scroll (decidir si hamburguesa o así está bien); (b) el isotipo está COPIADO: la fuente de verdad es `apps/landing/assets/logo-mark.svg` y `apps/web/public/icon.svg` + `icon-maskable.svg` lo reproducen a mano (documentado en los SVG: si el isotipo cambia, re-copiar y re-rasterizar los 7 PNG); (c) el wordmark del logo es `<text>`, no contornos — como `<img>` externo cae a la fuente de respaldo (documentado en el SVG) | Claude (cuando la marca lo pida) |
| DT-42 | 🟡 diferida (cosmética) | **El enlace de los correos de Auth sale por el dominio viejo**: `callbackUri` es `https://nutriscann-f809e.firebaseapp.com/__/auth/action`, así que quien reciba el enlace de entrada verá ese dominio un instante antes de aterrizar en la app. Lo demás del correo ya está bien: sale de **`noreply@caliscan.app`**, con SPF y los dos DKIM verificados (02/09), y el nombre que se muestra es **CaliScan** en el correo y en la pantalla de consentimiento de Google. **La API lo rechaza** (`EMAIL_TEMPLATE_UPDATE_NOT_ALLOWED`) y el enlace "Personalizar URL de acción" de la consola no se dejó usar el 02/09. No afecta al funcionamiento: el enlace lleva a donde tiene que llevar. Se reintenta cuando la consola lo permita | Tomás (consola) |
| DT-43 | 🔴 abierta (⚠️ trampa) | **NO marcar "enforce" de App Check para Firestore ni para Storage en la consola sin cambiar antes el front.** App Check está exigido (cuando se encienda) SOLO en `analyze`, que es donde se paga el modelo. La PWA, en cambio, lee `config/app` y da de alta en `waitlist/` **por REST y sin el SDK de Firebase** —decisión medida de la card 2.3: traer `firebase/firestore` costaba 110 kB gzip— y esas llamadas **no llevan la cabecera de App Check**. El día que alguien active el bloqueo de App Check para Firestore desde la consola, la app se queda sin textos y la lista de espera deja de recoger altas, sin ningún despliegue de por medio y sin un error que apunte a la causa. Si algún día se quiere ese bloqueo, primero hay que mover esas dos llamadas al SDK y pagar los 110 kB, con la decisión escrita | Quien toque la consola de App Check (declarado por la card 4.4) |
| DT-44 | 🟢 **desbloqueada 02/09 (WS10): el token real ya se vio** | **El ensayo que faltaba está hecho.** En el E2E del teléfono las tres peticiones a `analyze` pasaron **sin una sola advertencia de procedencia**, y eso prueba que llegó un token de App Check emitido por nuestra clave de reCAPTCHA: la procedencia se comprueba ANTES del cupo y `advertir` está cableado a `logger.warn` (`index.ts:167`) — el mismo logger que sí escribió el `cupo_agotado`. **Lo que queda es solo la decisión de encender `app_check_enforced`**, y hoy el riesgo es mínimo: hay **un solo usuario**, así que no hay teléfonos con versiones viejas cacheadas a los que echar. ⚠️ Sigue en pie la **DT-43**: esto es el interruptor de `analyze` en `config/app`, **NUNCA** el «enforce» de Firestore en la consola. Marcha atrás medida: **60 segundos** | Tomás (decisión) |
| DT-45 | 🟡 diferida | **El error de App Check vive fuera del catálogo de errores**: la card 4.4 no podía tocar `errores.ts` (era territorio de la 4.5, en paralelo), así que `app_check_invalido` tiene su propio código, su propio cuerpo y una rama especial en el `catch`. Funciona y está probado, pero son **dos catálogos de errores** en un proyecto cuya gracia es la lista cerrada. Receta escrita por la propia card: añadir `app_check_invalido` (403, `error_app_check`) a `ERRORES`, hacer que `ErrorDeAppCheck` extienda `ErrorDeAnalisis`, borrar de `procedencia.ts` las cuatro piezas duplicadas y la rama del `catch`, y **publicar `error_app_check` en `config/copy.json`** —hoy su texto no se puede cambiar sin desplegar, que es justo lo que DT-40 vino a arreglar. ⚠️ Al añadir la clave, el candado del seed FALLA hasta que se siembre: eso es que muerde | Claude, WS10 |
| DT-46 | 🟡 diferida | **El resto de la mudanza de textos a `config/copy.json`** (lo que DT-41 (b) dejó medido): términos, pantalla de entrada (`copy.auth.ts`), perfil, modales, navegación y pie. No es un problema de diseño sino de cableado: siete pantallas no reciben `copy` y pasárselo toca `App.tsx` (una línea por pantalla), más `lib/listaDeEspera.ts` y `lib/api.ts`, que leen textos por su cuenta. Emparentada con los T&C duplicados entre la app y la landing | Claude, WS10 |
| DT-47 | 🟡 abierta (producto) | **`plans_gold_highlight_body` dice «customizadas»**, un anglicismo: en España se dice «personalizadas». Se dejó byte a byte a propósito para que el diff de la mudanza fuera verificable. **Ya se cambia sin desplegar**, editando `config/app.copy` | Tomás (decisión de copy) |
| DT-48 | 🔴 abierta (WS10) | **El CI no corre los tests de `functions`.** El job `verify` de `.github/workflows/deploy.yml` corre `npm run lint` (solo tipos), los candados del catálogo, los tests del seed y el build — pero **`npm --prefix functions test` no está en ningún paso**. Son **455 de los 756 tests** del proyecto, y son justo los que cubren la Fase 4 entera: la verificación del token, el conteo transaccional del cupo y los cuatro estados de App Check. Hoy (02/09, WS10) se corrieron a mano antes del merge y dieron 455/455, pero eso es memoria de una sesión, no un candado: **hoy cualquiera puede romper el cupo y desplegarlo con el CI en verde**. Arreglo: un paso `npm --prefix functions test` en `verify`, después de `lint`. Los tests que necesitan emuladores ya se saltean solos cuando no están levantados, así que el paso corre tal cual en GitHub sin infraestructura nueva | Claude, WS11 |
| DT-49 | 🟡 abierta (medida) | **El peso del bundle declarado ya no es el real**: la card 4.1 declaró **114,2 kB gzip** al cerrar la Fase 4, y los dos commits visuales que la WS09 hizo después (`90eac7e` el logotipo, `1a5f3c7` el eslogan) lo dejaron en **116,65 kB** — medido en el build previo al merge del 02/09. Son 2,4 kB y ninguna alarma: se anota porque el proyecto viene midiendo cada kilobyte desde la card 2.3 (los 110 kB que costaba traer el SDK de Firestore), y una cifra declarada que nadie vuelve a medir deja de ser una vara | Claude, cuando toque el próximo cambio de front |
| DT-50 | 🟢 resuelta 02/09 (WS10), escrita porque REAPARECE | **Un `authDomain` propio obliga a dar de alta la dirección de retorno en el cliente OAuth de Google, a mano.** El E2E del teléfono falló con `Error 400: redirect_uri_mismatch`: la app pide entrar por `https://app.caliscan.app/__/auth/handler` (card 4.1, para que Safari y la PWA de iPhone no se rompan) y el cliente OAuth solo tenía autorizado `nutriscann-f809e.firebaseapp.com`. **Arreglo: añadir `https://app.caliscan.app` a los orígenes de JavaScript y `https://app.caliscan.app/__/auth/handler` a los URIs de redirección** del cliente `590554450084-5v9t8mksb2eu4nvdd7ntlsrt6asf7qhu`. ⚠️ **No hay API pública que lo edite** (se probaron tres caminos: `gcloud alpha iap`, `clientauthconfig`, Identity Toolkit): es consola, y por eso ninguna automatización lo va a atrapar. **Vuelve a morder** cada vez que se cambie el `authDomain`, se sume un dominio o se cree un cliente OAuth nuevo. Se comprueba sin gastar un intento desde el teléfono, pidiéndole a Google la URL de autorización con ese `redirect_uri` y mirando si responde `redirect_uri_mismatch` | Tomás (consola) |
| DT-26 | 🟡 diferida | **Huecos de curación en español que el motor nuevo destapó**: falta el alias "Lentejas" (el nombre es "Lentejas cocidas con sal y grasa" → 14 % en vez de 85 %) · "croquetas" gana el alias `Croqueta` de Buñuelo (0,6) sobre "Croquetas de papa" · no hay ficha de limón entero (`lemon` cae en Tarta de limón) · `tortilla, corn` resuelve a Tortilla de trigo (única regresión de la card 2.6; USDA la llama `Tortilla, NFS`) · `bread roll` da Pan (267 kcal) en vez de Panecillo (fdc-2707595, 279) — **REMEDIADO EL DIAGNÓSTICO, no el hueco (card 6.3, 01/09/2026): NO se arregla con un alias**, porque `aliases` tiene una sola clave, `es`, y del lado inglés el único vocabulario es `names.en`, que en las fichas de USDA lo escribe el CSV y la curación no pisa. Es del matcher o de una extensión del contrato. Verificado que sigue vivo: `bread roll` -> Pan a 0,26, mientras `panecillo` -> Panecillo a 0,85 · los nombres USDA de 3+ segmentos (`Spinach, fresh, cooked, fat added…`) siguen fuera del alcance de las variantes. Todo se arregla en `kb/curation/`, nunca en el motor. **Los otros dos pendientes que la card 6.1 dejó abiertos, medidos y resueltos por la card 6.3: `sweet corn, cooked` y `cabbage, cooked` NO son de curación sino de FICHA** — el catálogo no tiene ninguna ficha de maíz dulce (cero coincidencias en 1.036), y de repollo cocido solo tiene `Cabbage, green, cooked, **fat added**` (55 kcal) y la roja; el repollo hervido del cocido madrileño ronda las 23 kcal, así que colgarlo del de grasa añadida publicaría un +139 % con una reserva escrita al lado. Se decidió NO emitir el alias y mandarlos a DT-33: el silencio honesto es mejor que un número al doble | Tomás + Claude (curación) |
| DT-51 | 🔴 abierta (WS11) | **«Perrito caliente» resuelve a `Hot dog`, la salchicha sin pan.** Entra por un alias con confianza 1,0 escrito por la curación, y la regla de la contradicción de familia (card 5.3) solo desarma conjeturas del motor, nunca un término escrito. Se cierra con una guarda de vocabulario (`kb/curation/guardas.vocabulario.json`) o corrigiendo el alias hacia la ficha del bocadillo. Es el único de los 237 nombres de la taxonomía que cae en otra familia | Claude (curación) |
| DT-52 | 🟡 abierta (WS11) | **`Fish, NFS` (fdc, 238 kcal/100 g, 14,5 g de grasa) es un pescado rebozado disfrazado de promedio de familia** y es la cabeza de `pescado`. Pasa el candado de plausibilidad (sus números son posibles) pero está MAL ELEGIDA como representante: un pescado a la plancha ronda 120. Mismo caso que la DT-7 con el bacalao. Cambiar la cabeza en `familias.json` por una ficha de pescado cocinado sin rebozar, o deprecar la ficha | Claude (curación) |
| DT-53 | 🟡 abierta (WS11) | **Nueve ingredientes base que USDA SÍ mide y el catálogo no tiene** (Bloque 0 de la Fase 5, punto d): ensalada verde mixta, lechuga con verduras, té negro, crema de verduras, salsa blanca, queso fresco, pasas, dátil, orejones; más hojaldre, masa filo, pan rallado, sofrito, pasta de tomate y **pan de centeno** (`Bread, rye`: el escaneo del 02/09 cayó en `Pan` genérico). Es promoción de fichas por el pipeline de `kb/`, no curación a mano. Mientras tanto la masa de pizza y el mascarpone viajan como sustitutos declarados | Claude (kb) |
| DT-54 | 🟡 abierta (WS11) | **La cabeza de `empanada/masa-frita` es una samosa (310 kcal/100 g)** y la corrida v5 de la visión mandó ahí las croquetas (~240). El nombre exacto sigue ganando cuando llega, pero la cabeza está lejos del centro de su grupo. Elegir una cabeza más representativa o partir la subfamilia | Claude (curación) |
| DT-55 | 🟡 abierta (WS11) | **`etiqueta_del_envase` puede traer basura**: la foto 09 del golden devolvió `", the tag says"`. El saneo de `interpretarVision` solo recorta espacios. Si el motor la usa para buscar, hace falta un piso: largo mínimo, descartar lo que empieza con puntuación, y quizá exigir que contenga una letra | Claude (visión) |
| DT-56 | 🟡 diferida (WS11, producto) | **El vocabulario INGLÉS del catálogo es el canal débil**: 0 aliases en inglés contra 536 fichas con aliases en español, y `names.en` son descripciones de USDA («Alcoholic beverage, beer, regular, all»). Solo con el término inglés, 19 de los 68 ítems del golden v3 quedan sin ficha (beer, tuna canned, tomato raw…). Hoy lo salva el español; una versión en inglés de la app necesita aliases EN generados en el build (quitar los marcadores USDA) más curación con el molde del español | Claude (kb, v1.5 EN) |
| DT-57 | 🟡 diferida (WS11) | **Calibración de gramos de la visión**: el escaneo real del 02/09 estimó 400 g de huevo cocido (unos 7 huevos). ~~No hay foto guardada para verificarlo (`image_ref` sigue en `null`)~~ **DESTRABADA por la card 6.0 (WS12, 03/09): el expediente guarda la foto y la salida cruda del modelo.** Lo que sigue es la card 6.5 («el modelo cuenta, la base pesa») y el golden de peso conocido (6.6), ver `docs/bloque0.fase6.md` H7 | Claude (visión) |
| DT-58 | 🟡 abierta (WS11) | **La composición parcial y los sustitutos viven en el motor, no en el build**: `familias.json` se copia a `functions/src/kb/familias.ts` con candado, y las fichas de Firestore NO llevan `familia`/`subfamilia`. Funciona porque el motor resuelve por id, pero el día que el catálogo se re-sembre con familias en cada ficha (más limpio para la consola y para la curación), el build tiene que emitirlas y el seed publicarlas | Claude (kb) |
| DT-59 | ✅ cerrada 03/09 (WS13) | **Los Términos y Condiciones ya dicen qué se hace con la foto, la cuenta y el cupo.** Texto aprobado por Tomás (WS13): tres secciones nuevas (`tu_cuenta`, `tus_fotos`, `tu_opinion`) y la última reescrita (`lo_que_viene`, con la v1.5: recetas nuestras gratis y ES/EN). Cada afirmación corresponde a algo que el código hace hoy: la foto va al modelo de Anthropic solo para reconocer, se guarda con su expediente en Google Cloud en Europa (Firestore `eur3`, bucket `europe-west1`, verificado), la ve solo su dueño, se conserva mientras exista la cuenta y se borra a pedido. Dos copias que dicen lo mismo (`apps/web/src/lib/copy.terminos.ts` y `apps/landing/terminos.html`, paridad verificada párrafo a párrafo). Deja dos promesas que sostener a mano: el buzón (DT-67) y el borrado a pedido (DT-60) | Tomás + Claude |
| DT-60 | 🟡 abierta (WS12) | **Retención de fotos:** no hay regla de ciclo de vida en el bucket ni borrado de la foto al borrar un scan o una cuenta; quedan para siempre. Decidir la retención (¿90 días?) y aplicarla con una regla de lifecycle del bucket + borrado en cascada | Claude (infra) + Tomás (decisión) |
| DT-61 | 🟡 abierta (WS12) | **La cuenta de cómputo escribe en Storage de rebote:** `590554450084-compute@developer.gserviceaccount.com` solo tiene `cloudbuild.builds.builder` y `datastore.user`; sube y borra fotos porque el rol de build incluye `storage.objects.*`. El día que se ajuste IAM, las fotos fallan en silencio (`image_error` en el expediente). Dar `roles/storage.objectAdmin` explícito sobre el bucket | Claude (infra) |
| DT-62 | 🔵 abierta (WS12) | El `contentType` del objeto sale del `media_type` que DECLARA el cliente, no de los bytes: un cliente que diga `image/png` y mande JPEG deja un `.png` con el tipo mentido. Es lo mismo que se le manda a Anthropic; hoy no rompe nada. Verificar el primer deploy real mirando `image_ref`/`image_error` de un escaneo de producción, no el verde del CI | Claude |
| DT-63 | 🟡 abierta (WS12) | **Vocabulario que la Fase 6 destapó** (card 6.4): sin alias «atún en lata» / «atún en conserva» (cae en `Atún` genérico a 0,185–0,267), «huevo duro» (0,3), «lombarda»/«col lombarda» (sin ficha), `canned tuna`, `hard-boiled egg`, `tomato, chopped` (plural `Tomatoes`), `cabbage, cooked`; `arroz`/`rice` a secas caen en «Arroz rojo»/«Sopa de arroz»; no hay ficha de champiñón crudo llano (solo shiitake) ni de maíz dulce; la cabeza de `pescado/conserva` es «Sardinas en lata» (208 kcal) | Claude (curación) |
| DT-64 | 🟡 abierta (WS12) | **El catálogo no declara qué alimentos se comen crudos** y la densidad no lo suple cuando cocinar AGREGA grasa (huevo 143→176, papa 77→126: la misma forma que la lechuga 20→49). La card 6.3 lo resuelve por familia (verdura y fruta se comen crudas); un campo declarado por ficha sería más fino. Y el arreglo estructural: el componente ya viaja con `verdura/cruda` declarado por la visión y el matcher no lo usa como estado pedido | Claude (motor + curación) |
| DT-65 | 🔴 abierta (WS12) | **La compuerta del 12 % ya no corta el caso que la creó**: con la cascada de la card 5.3, la comida de plástico (golden 28) publica 1.098 kcal como total completo vía cabeza «Crepe» a 0,306, mientras la ensalada mixta (golden 20) cae a 0,107. Medido en `docs/bloque0.fase6.md` H2. Es la card 6.2, que espera a la 6.1 y a la 6.3 | Claude (motor) |
| DT-66 | 🟡 abierta (WS12) | **Al responder por cabeza se pierde la preparación:** `white rice, fried` cae en «Arroz blanco cocido» (producción 03/09, arroz frito con verduras). La cabeza contesta la subfamilia, no el método que dijo la visión; aplicar la transformación de cocción declarada sobre la cabeza, o elegir la cabeza de la subfamilia `frito` cuando exista | Claude (motor) |
| DT-67 | 🔴 abierta (WS13) · **BLOQUEA EL DEPLOY de la Fase 6** | **El buzón `soporte@caliscan.app` no existe todavía.** Los T&C (DT-59) lo publican como la vía para pedir el borrado de la cuenta; un correo que rebota es peor que ninguno. Crearlo con Cloudflare Email Routing (el dominio ya está en Cloudflare) reenviando al Gmail de Tomás, y probarlo mandando un correo ANTES de mergear `fase/06` a `main` | Tomás |
| DT-68 | 🔵 abierta (WS13) | **Motivo equivocado cuando todos los gramos de una composición son ≤ 0 o NaN:** `componerPlato` chequea `resueltos.length === 0` antes que `algunoSinGramos`, así que si los ingredientes SÍ están en el catálogo pero llegan sin gramos válidos, el motivo dice «el catálogo no tiene ninguno de sus ingredientes», que es falso. Pre-existente de la card 5.3, lo encontró el Q/A de la 6.1. Invertir el orden de los chequeos y un test que lo muerda | Claude (motor) |

---

## DT-1 · Activar el plan Blaze del proyecto ✅ (31/08/2026)

**Cerrada.** Tomás activó Blaze el 31/08/2026; verificado con `gcloud billing projects
describe` (`billingEnabled: true`). El orden de destrabe previsto se cumplió: DT-4
(presupuesto) se creó el mismo día. Quedan DT-2 y DT-3, y ampliar el objetivo del
pipeline de deploy. Lo que sigue es el registro original de la deuda.

**Qué falta.** El proyecto `nutriscann-f809e` está en plan Spark (`billingEnabled: false`).
Cloud Functions, Secret Manager y Cloud Storage requieren **Blaze** (pago por uso).

**Qué bloquea.** Todo el backend. Sin esto no hay motor de análisis: la Fase 2 no puede
desplegarse y la pantalla de fundaciones seguirá diciendo "Backend no disponible".
La **Fase 1 (base de conocimiento) NO está bloqueada** — se puede construir entera,
porque el catálogo se compila en el repo y Firestore ya existe.

**Por qué no lo hizo Claude.** Vincular una cuenta de facturación es una decisión con
consecuencias económicas: la toma Tomás, no un agente.

**Cómo se destraba.** Consola de Firebase → Configuración del proyecto → Uso y facturación
→ Modificar plan → Blaze, y vincular la cuenta `0151E4-2765DB-FC9340` (ya abierta).

**Costo esperado.** Prácticamente cero con el volumen inicial: el nivel gratuito de Blaze
cubre de sobra el uso de desarrollo. Lo que sí cuesta es la API de Anthropic, ~$0,01 por
análisis, y eso se factura aparte.

**Al destrabarse, en orden:** DT-4 (presupuesto, primero) → DT-2 → DT-3 → desplegar
`health` → ampliar el objetivo del pipeline a `hosting,firestore,storage,functions`
(la línea ya está comentada en `.github/workflows/deploy.yml`).

---

## DT-2 · API key de Anthropic en Secret Manager ✅ (02/09/2026)

**Cerrada en la WS08 y verificada de nuevo en el Bloque 0 de la WS09**, esta vez
contra la API de GCP y no contra el recuerdo: el secreto `ANTHROPIC_API_KEY` existe
en el proyecto (creado el 02/09 a las 06:57), la función `analyze` lo declara en su
`onRequest` y `health` responde en producción con el catálogo publicado. El valor
nunca pasó por el orquestador: lo cargó Tomás.

---

## DT-3 · Inicializar Cloud Storage ✅ (02/09/2026)

**Cerrada en la WS08 y verificada de nuevo en el Bloque 0 de la WS09:** el bucket
`nutriscann-f809e.firebasestorage.app` existe **en europe-west1** (la misma región que
las funciones) y el CI incluye `storage` entre sus objetivos de despliegue, así que
`storage.rules` sale con cada merge a `main`.

⚠️ La lección de la WS08 vale para cualquier recurso nuevo: el "Comenzar" de la consola
creó el bucket en **US-WEST1** porque el selector viene con EE.UU. por defecto. Se
detectó recién creado y vacío y se recreó por API en la región correcta. Un bucket de
fotos al otro lado del Atlántico de las funciones que las leen es latencia y es factura.

---

## DT-4 · Presupuesto de GCP con alertas ✅ (31/08/2026)

**Cerrada el mismo día que DT-1, como estaba previsto** ("un proyecto que puede
facturar sin techo declarado es un riesgo evitable"). Creado vía `gcloud billing
budgets create`: **"NutriScann — presupuesto mensual", €10/mes, alertas al 50 % y
al 90 %**, filtrado solo al proyecto `nutriscann-f809e` (los otros presupuestos de
la cuenta quedaron intactos). Las alertas llegan por correo a los administradores
de la cuenta de facturación.

---

## DT-5 · Íconos de la PWA 🟡

`manifest.webmanifest` declara `"icons": []`. La app se instala igual, pero sin ícono
propio. Se resuelve en la Fase 3, cuando exista identidad visual definitiva.

---

## DT-27 · Lote de fichas nuevas v1.1 — insumos de Tomás (01/09/2026) 🟡

**Regla de la card:** los números salen de USDA (o fuente citable declarada, como el
salmorejo); los insumos de abajo sirven de **contraste y selección**, no de fuente.
De Tomás falta lo que USDA no sabe: porciones de España y vocabulario de barra.

**🍺 Cerveza** — datos aportados (por 355 ml): regular 153 kcal · 12-13 g carbohidratos ·
1,6 g proteína · 0 grasa. Rangos por tipo: 0,0 % 18-37 · light 100-110 · regular
150-180 · artesanal/IPA 180-250 · negra 200-300. **Contraste con USDA: 153/355 ml =
43,1 kcal/100 ml — coincide exacto con "Alcoholic beverage, beer, regular" de SR.**
Selección propuesta: 3-4 fichas (regular · light/0,0 · IPA/artesanal · negra), caveat
de "calorías del alcohol" (7 kcal/g, el candado de Atwater ya lo contempla).
Pendiente de Tomás: porciones reales de España (¿caña 200 ml? ¿tercio 330? ¿doble?
¿pinta 500?) para los `portion_hints`.

**🍋 Limón** — datos aportados (por 100 g comestible): 29 kcal · 9,32 g carbohidratos
(2,5 azúcares) · 2,8 fibra · 1,1 proteína · 0,3 grasa · vit. C 53 mg. **Contraste:
coincide con "Lemons, raw, without peel" de USDA (29 kcal/100 g) — la fuente Wikipedia
casi seguro bebe de USDA.** Ficha directa de SR; decidir si además hace falta "zumo de
limón" (la rodaja del agua o la ensalada pesa ~5-10 g: el impacto calórico es ~0).

**⚠️ Nota de marca:** una de las fuentes citadas por Tomás es `nutriscan.app` — la
marca "NutriScan App" ya detectada en la WS03. Existe y publica contenido nutricional
en español: tenerlo presente para la decisión de naming antes del lanzamiento.

**🌽 Tortilla de maíz** — datos aportados (por pieza de 30 g): 60-65 kcal ·
12-13,5 g carbohidratos · 1,4-1,7 proteína · 0,5-1 grasa · 1-1,5 fibra · sodio <5 mg.
**Contraste: coincide con USDA ("Tortillas, corn" ~218 kcal/100 g × 30 g ≈ 65).**
Ficha directa de USDA + porción "1 pieza (30 g)"; de paso resuelve la única regresión
de la card 2.6 (`tortilla, corn` → Tortilla de trigo, DT-26).

**🫓 Arepa** — datos aportados (asada simple, por 100 g): 168-215 kcal · 35-40 g
carbohidratos · 3,5-5 proteína · 0,6-1 grasa · 2,5-3,5 fibra. **Caso especial: es la
única del lote SIN ficha USDA posible** (verificado en los dos golden tests: el
"no_catalogado" de la arepa era correcto). Dos caminos para la card: (a) **receta
compuesta** (mecanismo 1.7: harina de maíz precocida USDA + agua + sal + rendimiento
de plancha 0,757 medido) — preferible porque hereda provenance USDA; (b) curación
manual con fuente web declarada (precedente salmorejo). El rango aportado por Tomás
queda como contraste para validar la derivación: si la receta no cae en 168-215
kcal/100 g, algo está mal.

**🍺 Cerveza — porciones y vocabulario de España (aportado por Tomás, 01/09):**
*De grifo:* corto/zurito/penalti 100-140 ml (Castilla y León-Galicia-Rioja / País
Vasco / Aragón) · **caña ~200 ml (la estándar; en el sur/levante hasta 250-300)** ·
doble/cañón ~400 ml · tubo ~330 ml · jarra/tanque/maceta 500 ml. *De botella:*
quinto/botellín 200 ml · **tercio/mediana/media 330 ml** · litrona 1.000 ml
(cachi/katxi/mini en vaso de fiesta). → `portion_hints` de las fichas de cerveza
(etiqueta + gramos, densidad ≈ 1) y aliases regionales con confianza; "caña" y
"tercio" como porciones por defecto de grifo y botella. Con esto la card tiene
TODO lo que USDA no sabía; queda solo ejecutarla (WS06).

**🥃🍷 Tragos (aportado por Tomás, 01/09)** — destilados 45 ml: 65-100 kcal (0
carbohidratos) · cerveza 330 ml: 150-160 · vino copa 150 ml: 65-85 · cócteles:
160 (cuba libre) a 400+ (con cremas). Fuente destacable: Ministerio de Sanidad
(estilosdevidasaludable.sanidad.gob.es), la misma familia que el PDF SENC de v2.
**Contraste USDA: destilados ✓ (80-proof ≈ 104 kcal/45 ml) · cerveza ✓ (~145/330) ·
⚠️ vino NO cruza: USDA da ~83 kcal/100 ml → una copa de 150 ml son ~125 kcal, no
65-85 (ese rango parece ser por 100 ml). Al armar la ficha, ganan los números USDA
y la porción se declara explícita.** Alcance: fichas simples (destilado genérico,
vino tinto/blanco) en el lote v1.1; los cócteles son candidatos a receta compuesta
(cuba libre = ron + refresco de cola, mecanismo 1.7) — v1.1+, no urgente.
